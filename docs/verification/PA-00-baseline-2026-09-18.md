# PA-00 baseline — 2026-09-18

Status: diagnosed; not a completion or runtime claim.

At `fb5d5717e82e36d8d5ac0085746e5fa864fcc70a`, local Python 3.10.12 ran:

```text
PYTHONPATH=src PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest -q \
  tests/test_prm_product_ux_eval.py::test_product_ux_keeps_project_context_for_confirmation_followups
```

Result: `1 passed in 6.56s`. The historical GitHub record (`35328205490`,
302 passed / 1 failed) is therefore not reproduced locally and is not treated
as current runtime proof.

Static diagnosis finds both required PA-00 correction surfaces:

- `tools/prm_product_ux_eval.py` creates `eval-*` action-context IDs and turns
  a free-text post-answer action into a simulated preview from dialogue state.
- `src/bot/prm_handlers.py` resolves live free text from dialogue state and
  calls the post-answer callback with a synthesized `actor_id=chat_id`.

PA-00 must split the evaluator into denied legacy free-text and explicit
bound-inline fixture branches, then remove the unsafe live text path. No
Telegram, provider, production database, service, timer, or private archive
content was accessed by this diagnosis.
