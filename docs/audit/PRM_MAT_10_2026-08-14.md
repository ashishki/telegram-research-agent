# PRM-MAT-10 implementation evidence

Status: fixture-first verification planning only. No live fetch, DNS lookup,
redirect, cache write, provider call, network access, or canonical archive
change was enabled.

The plan now accepts only HTTPS candidate URLs and excludes private, loopback,
link-local, and reserved IP literals. A hostname is never official merely
because it contains `docs`, starts with `www`, or resembles GitHub: only
`github.com` / its subdomains or an explicit `official_relation=true` are
planned as official sources. Telegram remains discovery context and all plans
retain the visible approval-required boundary.

Verification: `PYTHONPATH=src python3 -m pytest
tests/test_primary_source_verification.py -q` was later included in the
targeted Block-D correction set — `108 passed in 13.66s` together with the
MAT-12 integration and Telegram renderer tests;
Playbook validation passed with `errors=0 warnings=0`; `git diff --check`
passed. No full suite was run under the global operator policy.

Live fetch, trusted host/source records, cache retention, and fetch/provider
budget remain explicit human approvals. MAT-10 does not grant any of them.
