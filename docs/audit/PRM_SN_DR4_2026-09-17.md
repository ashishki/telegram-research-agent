# PRM-SN-DR-4 — phase-4 engineering receipt

Gate/scope: explicit, fixture-only on-demand topic edition: event projection,
version/change semantics, source coverage, editorial ranking and final Telegram
plain-text rendering. No pilot, runtime collection, provider call, delivery,
private archive, production write or release was reviewed or authorised.

Base SHA: `cee8baae3b8a41f571bd689f2dadf7e6e981863f`.
Final reviewed accumulated diff SHA-256:
`74289978be3e8bd6b36d60f6c4eadbe40e307bb2df3f69524d7eb8c8335051a8`.

Requested command:

```text
codex exec --cd /tmp/prm-sn-dr4-proof/input --skip-git-repo-check
  --ignore-user-config --ephemeral --sandbox read-only --model gpt-5.6-terra
  -c model_reasoning_effort="high" --output-last-message …/review.md
```

Observed runtime evidence: `runner.stderr.log` banner recorded Codex `v0.154.0`,
model `gpt-5.6-terra`, sandbox `read-only`, reasoning effort `high`, and session
`01a0b061-2846-77d3-aa02-0259b253b839`. The sanitized runner logs remain outside
the repository. Exit completed successfully and review returned
`PACKET_REVIEW_RESULT: PASS`.

Review progression and closure:

- Initial review found five P1s in pure/change semantics, freshness/provenance,
  source health/provenance, cap/omissions and editorial sections.
- Correction review found five remaining P1 boundary cases. Final correction
  re-verification found invalid-window final text, count-cap arithmetic and
  application-level ranking P1s.
- Final proof review confirmed all three were closed: invalid/reversed windows
  return `invalid_edition_window`; omissions are exact under both caps; the
  application path projects, ranks and groups before rendering. It also found
  no collector, delivery, job, durable write, fetch or egress path.

Implementer verification:

```text
PYTHONPATH=src PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest \
  tests/test_prm_application.py tests/test_external_watch_shadow.py \
  tests/test_external_watch_selection.py tests/test_external_watch_delivery.py -q
38 passed in 4.64s
```

Engineering scope passed. Human usefulness labels, real source coverage,
provider/runtime behavior, pilot approval and release remain separate pending
gates. Rollback is feature-off/no invocation of `render_topic_edition`; no
stored state or job was created by this phase.
