# PRM-MAT-1 completion evidence

Status: implementation and deterministic verification complete; this is not
operator validation, dogfood evidence, a release claim, or authorization for a
durable profile/session/configuration write.

## Scoped outcome

- Added the in-memory-only `operator_context.v1` contract and validation.
- Auto text and transcribed voice requests select one workflow before local
  retrieval. The selected context carries an opaque interaction ID and a
  hashed chat identity; it does not enable durable writes.
- Current-fact and unsupported project-state gates override a requested chat
  route and stay on the local research/verification boundary.
- The voice dispatcher preserves `voice_transcript` as the input kind through
  the auto route.

## Changed files

- `src/assistant/operator_context.py`
- `src/bot/bot.py`
- `src/bot/handlers.py`
- `tests/test_operator_context.py`
- `tests/test_handlers.py`
- `tests/test_callbacks.py`

## Verification

- Targeted route/context tests: `PYTHONPATH=src python3 -m pytest
  tests/test_operator_context.py tests/test_handlers.py
  tests/test_callbacks.py::TestIdeaCallbacks::test_run_bot_prm_safe_dispatches_transcribed_voice_as_auto
  -q` — `59 passed in 21.05s`.
- A broader `fast-contract` tier was started before the operator narrowed the
  test policy, but its result was not retained and is not used as evidence.
- Contract validator: `python3 tools/playbook_validate.py --root . --check
  tasks --check references` (passed with `errors=0 warnings=0`).
- Full repository verification was intentionally stopped before completion on the
  operator's instruction to use only targeted tests; it has no result and is
  not evidence for MAT-1.
- Whitespace check: `git diff --check`.

The targeted tier is sufficient here because MAT-1 is a narrow in-memory
routing contract. A full suite is reserved for a block review, shared-core
regression symptoms, or an explicit operator request. A block deep review is
not yet due because PRM-MAT block A closes only after MAT-5. MAT-1 changes no
persistent schema, write, provider/raw-text egress, network, schedule, trust,
backup, dogfood, release, or compatibility boundary, so no immediate
deep-review trigger applies.

## Required follow-up and approvals

PRM-MAT-2 may consume this ephemeral context. PRM-MAT-3 remains blocked on the
explicit human approval of the exact Portfolio V2 descriptor and active/priority
set; this implementation grants no such approval.
