# PRM-SN-DR-5 — phase-5 engineering receipt

Gate result: **PASS** for the exact fixture-only engineering scope. This receipt
does not authorize a pilot, runtime/service action, Telegram delivery, provider
or private-data egress, production migration, dogfood, release, commit, push or
merge.

Base SHA: `cee8baae3b8a41f571bd689f2dadf7e6e981863f`.
Final accumulated source/test diff SHA-256:
`d0fecb9273c769a56c3ff081fe3b410eb7389b3c1f58933c5fde3300d7b0ce52`.
The reproducible tracked-plus-untracked derivation and source inventory are in
`PRM_SN_DR5_SCOPE_2026-09-17.json`; it excludes `__pycache__`/`.pyc` artifacts.

Independent read-only review:

```text
codex exec --sandbox read-only --model gpt-5.6-terra \
  -c model_reasoning_effort="high" …
```

Observed runner metadata: Codex `v0.154.0`, model `gpt-5.6-terra`, sandbox
`read-only`, reasoning effort `high`, session
`01a0b107-39ea-7b23-841c-ca4cf54b65e3`; completed successfully. Sanitized
runner artifacts remain outside the repository at
`/tmp/prm-sn-dr5-close-o3aQL6/`.

Review progression and closure:

- Earlier DR-5 reviews found source-revision loss, incomplete English rendering,
  stale packet provenance, activation-effect wording, terminal delivery TOCTOU,
  off-policy URLs, preview cancellation races, feedback-language gaps and
  non-reproducible scope evidence.
- Final code preserves the canonical profile revision through source → selection
  → outbox; reloads profile/kill controls at the terminal send boundary; only
  renders allowlisted source links; keeps unknown sends non-replaying; and makes
  confirmation previews transactionally claimed with resumable idempotent
  `confirming` recovery.
- English alerts, digests, feedback controls, callback toast and failure path are
  English-aware. The systemd template reads controls from `EnvironmentFile` and
  does not hard-code delivery enablement or the kill switch.
- The final reviewer verified base/hash, reverse-apply, whitespace, packet
  provenance and the resulting P0/P1 closure. It did not execute tests or any
  live action.

Implementer evidence before final review:

```text
PYTHONPATH=src PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest \
  tests/test_callbacks.py tests/test_external_watch_delivery.py \
  tests/test_utd_profile.py tests/test_prm_utd_callbacks.py -q
72 passed in 20.36s
git diff --check
passed
```

Residual gates are intentionally separate: human labels, observed service/profile
state, approved pilot start, real receipts, reconciliation/rollback exercise,
and release/rollout authority. The prepared pilot/rollback packet remains
`docs/PRM_SEARCH_NEWS_PILOT_PACKET.md` and requires a separate human decision.
