# PRM-SN-DR-1 — engineering review receipt (sanitised)

Gate and scope: PRM-SN-DR-1; final citation integrity, verification
completeness, critical factual slots, and safe archive/current partial output.
This packet contains public code and synthetic fixtures only. It excludes
credentials, `.env`, Telegram archive, databases/backups, services and sockets.

## Reviewed identity and verification

- Base SHA: `cee8baae3b8a41f571bd689f2dadf7e6e981863f`.
- Final reviewed working code/test diff SHA-256:
  `2efdd09b347f17294261b61e17ebd6986d546f54edd72857a9046ba1ef7be8dc`.
- Focused local command: `PYTHONPATH=src PYTEST_DISABLE_PLUGIN_AUTOLOAD=1
  python3 -m pytest tests/test_claim_ledger.py tests/test_prm_synthesis.py
  tests/test_prm_application.py tests/test_prm_bot_dispatch.py
  tests/test_prm_intent_archive_contract.py -q && git diff --check`.
- Result after final corrections: `45 passed in 3.05s`; whitespace check passed.
- Final reviewer input: isolated `/tmp/prm-sn-dr1-final.8K6Hp4` snapshot,
  public source and synthetic tests only; its manifest records the base and
  final working-diff hash above.

## Independent reviewer provenance

Final closure command (raw runner output remains outside the worktree):

```text
codex exec --cd /tmp/prm-sn-dr1-final.8K6Hp4 --skip-git-repo-check
  --ignore-user-config --ephemeral --sandbox read-only --model gpt-5.6-terra
  -c model_reasoning_effort="high" --output-last-message …/result.md - < …/review.md
```

- Requested model/effort: `gpt-5.6-terra` / `high`.
- Observed runner banner: Codex `v0.154.0`, model `gpt-5.6-terra`, provider
  `openai`, sandbox `read-only`, reasoning effort `high`, session
  `01a0afbc-dedc-71b3-8e9e-743b22cf66a8`; runner completed successfully.
- Final reviewer result: `PACKET_REVIEW_RESULT: PASS`.
- Reviewer checked static code/tests only and did not run tests, write files,
  access network, or receive private archive data.

## Findings, corrections and closure

Earlier independent static sessions found no P0, and found P1 defects in
visible-source substitution, claim-tail coverage, weak critical slots, archive
renderer bypass, detached source inventories, incomplete citation coverage,
lower-case Cyrillic actors, current-fact predicate bypass, Unicode fallback
splitting, and broad boundary classification. The final P2 was a receipt that
could describe the pre-fallback rather than returned text.

Corrections bind source URLs only when displayed, require complete final
verification/citation support, add bounded critical slots and useful
source-attributed fallback, limit nearby source binding to labelled lines,
narrow the boundary allow-list to explicit negative templates, and recompute
verification plus publication state after fallback replacement. Adversarial
fixtures cover a current price prefixed by `Внешняя проверка`, `Что известно
сейчас`, and `Следующий шаг`; each remains an unsupported source fact.

The final reviewer specifically confirmed that the adversarial price remains
`source_fact` and unsupported, the deterministic current boundary is usable
without factual claims, and returned fallback receipt state uses reverified
output.

## Result and residual gates

Engineering scope **passed** for the recorded diff. This does not create human
gold labels, provider/runtime measurements, external verification, pilot
authority, production permission, release claim or dogfood evidence. Those
remain separate gates. The correction budget was reassessed after repeated
findings; the final minimal public snapshot supplied explicit manifest and
targeted closure evidence rather than another broad self-approval loop.
