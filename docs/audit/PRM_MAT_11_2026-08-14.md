# PRM-MAT-11 implementation evidence

Confirmed append-only saved objects are now queryable as read-only secondary
evidence: topic/project/date/state filters return cited memory objects with
history, while archive evidence remains first in the context pack. `source_card`
is supported with an upgrade-safe SQLite CHECK rebuild and index recreation.
Confirmation timestamps and legacy offset timestamps are canonicalized to UTC
before date filtering. No automatic chat-to-memory conversion, production DB
write, network/provider call, or dogfood/release action occurred.

Verification: `PYTHONPATH=src python3 -m pytest tests/test_saved_knowledge.py
tests/test_memory_research.py::TestMemoryResearch::test_confirmed_saved_memory_is_secondary_to_archive_evidence -q`
— `5 passed in 23.82s`; `git diff --check` passed. Focused tests only; no full
suite was run. Final read-only Block B re-review found no actionable findings;
effective reviewer assignment was unverified.
