# PRM-MAT-12 implementation evidence

Status: deterministic workflow-section integration only; no provider synthesis,
live verification, database write, build/MVP approval, or release claim.

`professional_answer.v1` now carries one validated `workflow_section` from
the selected professional workflow. `memory_research` deterministically maps
the operator workflow to a matching derived professional section, preserves
the same interaction ID, and the Telegram reader renders one safe public
workflow focus when that section exists. The DTO validator rejects a
missing/non-object section; the existing action guards remain responsible for
direct evidence/current-fact boundaries.

Verification: `PYTHONPATH=src python3 -m pytest
tests/test_prm_professional_workflows.py tests/test_memory_research.py
tests/test_handlers.py -q` — initially `100 passed in 9.87s`, then the
Block-D correction set including MAT-10 reported `108 passed in 13.66s`.
No full suite was run.

Provider usage remains separately approval-gated. Block D deep review is due
after this MAT-10/MAT-12 pair.
