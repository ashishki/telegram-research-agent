anthropic>=0.40.0
openai>=1.54.0
pytest>=8.0.0
telethon>=1.36.0
scikit-learn>=1.4.0
pyyaml>=6.0.0
jsonschema>=4.18.0
numpy>=1.26.0
jinja2>=3.1,<4
weasyprint>=61,<63
# weasyprint 62.x breaks with pydyf >= 0.11; pin the compatible range.
pydyf>=0.10,<0.11
# Local PDF inspection/rasterization for the quality judge (no system deps).
pypdf>=4,<7
pypdfium2>=4,<6
# Optional external OCR cross-check transport (lazy import in the tool).
requests>=2.31,<3
